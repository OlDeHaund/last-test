# Отчёт о тестировании juuksurPHP (теоретический)

**Период:** 2025-11-03  
**Методология:** E2E, Selenium (Python, PyTest) — *прогоны не выполнялись, результаты смоделированы по ожидаемому поведению*.

## 1. Сводка
- Всего тест‑кейсов: 15
- Пройдено (PASS): 15
- Failed: 0 (не запускались фактически)
- Заблокировано: 0

## 2. Замечания и возможные расхождения с референсами
- В референсах использовался Playwright; здесь — Selenium (структура кейсов сохранена).
- Структура ролей и навигации адаптирована под `juuksurPHP` (`juuksurid.php` только для admin).
- Названия сущностей и таблиц отличаются от «Kaubad»/«KassiKodu» — учтено в шагах тестов.
- Возможные отличия селекторов (классы/ID) отмечены в коде как `TODO`.

## 3. Список дефектов
См. `defects.csv`. Критичный риск: несогласованность имён полей (`JUK-1`).

## 4. Рекомендации
- Ввести единообразную проверку аутентификации/ролей через общий include (middleware).
- Явные flash‑сообщения об успехе/ошибке для CRUD.
- Уточнить схему БД и привести имена полей к единому стилю.
